<?xml version="1.0" encoding="UTF-8"?>
<!--
  Optional reconciliation sweep for macOS (launchd).
  Requires: pip install morpheus-dreaming

  1. Set the python path below if `python` isn't on launchd's PATH.
  2. Copy to ~/Library/LaunchAgents/com.kumouri.claude-morpheus.plist
  3. launchctl load ~/Library/LaunchAgents/com.kumouri.claude-morpheus.plist

  Runs every 1800 seconds (30 min). The hooks already dream on compaction/end;
  this only catches crashed/archived sessions.

  Upgrading from a previously-installed agent registered under com.ceryce.claude-morpheus:
  launchd keys agents by the Label baked into the copy you already loaded, so overwriting
  this template does not migrate it. Before loading the new file, retire the old one:
    launchctl unload ~/Library/LaunchAgents/com.ceryce.claude-morpheus.plist
    rm ~/Library/LaunchAgents/com.ceryce.claude-morpheus.plist
  then follow steps 1-3 above with the new com.kumouri.claude-morpheus name.
-->
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN"
  "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <key>Label</key>
  <string>com.kumouri.claude-morpheus</string>
  <key>ProgramArguments</key>
  <array>
    <string>python</string>
    <string>-m</string>
    <string>morpheus</string>
    <string>reconcile</string>
  </array>
  <key>StartInterval</key>
  <integer>1800</integer>
  <key>RunAtLoad</key>
  <true/>
</dict>
</plist>
